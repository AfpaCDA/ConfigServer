spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9000/auth/realms/inscription-portal/protocol/openid-connect/certs

spring:
	jpa:
		properties:
			hibernate:
				dialect: org.hibernate.dialect.PostgreSQLDialect
		hibernate:
			ddl-auto: none
			show-sql: true
spring:
	datasource:
		url: jdbc:postgresql://localhost:5432/postgres
		username: Afpa
		password: 10cd@_@tos!